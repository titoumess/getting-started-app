name: build-docker
on: [push]
jobs:
  build-docker-image:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v4


###- checkout code
###- ls
###- docker build
###- docker images
###- docker push
###(pas de credentials en clair)
